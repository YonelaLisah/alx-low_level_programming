#include <stdio.h>
/**
 * main - An entry for a program that prints a line with the function printf
 * Return: 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
		return (0);
}
